import {heroes} from '../data/heroes';
//Con esta funcion se redirecciona a una persona y de manda a llamar en heroPage
export const getHeroById =(id)=>{
    return heroes.find( hero => hero.id === id);
}






























































































